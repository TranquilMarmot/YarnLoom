{
  "name": "loom-common",
  "version": "1.0.0",
  "description": "",
  "author": "",
  "license": "ISC",
  "scripts": {
    "build": "rm -rf out && tsc -p ./ && cp package.json ./out"
  },
  "engines": {
    "vscode": "^1.44.0"
  },
  "dependencies": {
    "typesafe-actions": "^5.1.0"
  }
}
